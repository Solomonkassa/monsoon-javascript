let name = 'xoraus'; // String Literal
let age = 30; // Number Literal
let isApproved = false; // Boolean Literal
let firstName = undefined; // if declared just firstName; then it's undefined by default
let selectedColor = null; // null in situations where we want to clear the value of a variable. (eg. from color red to null)


// In js, all numbers are of type numbers - no float etc

console.log(typeof 'xoraus') // string
console.log(typeof 5) // number
console.log(typeof true) // boolean
console.log(typeof null) // object type
console.log(typeof undefined) // undefined

// In static languages, when we declare a variable, the type of the variable is set and it cannot be changed in future

// In a dynamic language like JavaScript, the type of a variable can change at run time.
